import os from telegram import Update, Bot from telegram.ext import Updater, CommandHandler, MessageHandler, Filters, CallbackContext import openai # قراءة التوكن والمفتاح من المتغيرات البيئة BOT_TOKEN = os.getenv("BOT_TOKEN") OPENAI_API_KEY = os.getenv("OPENAI_API_KEY") openai.api_key = OPENAI_API_KEY # دالة للرد على الرسائل def handle_message(update: Update, context: CallbackContext): user_text = update.message.text # استدعاء OpenAI للرد الذكي response = openai.Completion.create( model="text-davinci-003", prompt=user_text, max_tokens=150 ) reply_text = response.choices[0].text.strip() update.message.reply_text(reply_text) # دالة بدء البوت def start(update: Update, context: CallbackContext): update.message.reply_text("مرحبًا! أنا بوت ذكي 🤖") def main(): updater = Updater(BOT_TOKEN, use_context=True) dp = updater.dispat
